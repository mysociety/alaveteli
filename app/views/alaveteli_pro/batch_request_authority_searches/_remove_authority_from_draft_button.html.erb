<%= form_tag(update_bodies_alaveteli_pro_draft_info_request_batch_path(draft.id),
             :method => :put,
             :class => 'batch-builder__list__item__action js-remove-authority-from-batch-form',
             'data-body-id' => authority_id) do |f| %>
  <% unless query.blank? %>
    <%= hidden_field_tag(:authority_query, query) %>
  <% end %>
  <% unless page.blank? %>
      <%= hidden_field_tag(:page, page) %>
    <% end %>
  <%= hidden_field_tag(:remove_body_id, authority_id) %>
  <button type="submit"
          class="js-remove_authority-from-batch-submit button--remove">
      <%= _('Remove') %>
  </button>
<% end %>
