<%= form_tag(remove_body_alaveteli_pro_draft_info_request_batch_path(draft.id),
             :method => :put,
             :class => 'batch-builder__authority-list__authority__action js-remove-authority-from-batch-form',
             'data-body-id' => authority_id) do |f| %>
  <% unless query.blank? %>
    <%= hidden_field_tag(:query, query) %>
  <% end %>
  <%= hidden_field_tag(:public_body_id, authority_id) %>
  <input type="submit"
         value="<%= _('- Remove') %>"
         class="js-remove_authority-from-batch-submit">
<% end %>
