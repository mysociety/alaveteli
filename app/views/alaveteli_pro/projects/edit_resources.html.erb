<% content_for :javascript_head do %>
  <%= javascript_importmap_tags("public") %>
<% end %>

<div id="project-form" class="inner-canvas">
  <div class="inner-canvas-header">
    <div class="row">
      <% @title = _("Add requests to your project") %>
      <h1><%= @title %></h1>
    </div>
  </div>

  <div class="inner-canvas-body">
    <div class="row">
      <%= foi_error_messages_for :project, full_message: true %>
    </div>

    <div class="row">
      <%= form_for([:alaveteli_pro, @project], data: { controller: 'projects--resources' }) do |f| %>
        <%= hidden_field_tag :page, current_page %>
        <%= hidden_field_tag :step, current_step %>

        <div id="project-resources">
          <%= render partial: 'resources', locals: { f: f } %>
        </div>
      <% end %>
    </div> <!-- .row -->
  </div> <!-- .inner-canvas-body -->
</div> <!-- #project-form.inner-canvas -->
