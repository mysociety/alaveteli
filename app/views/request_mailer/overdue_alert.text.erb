<%= _('{{authority_name}} have delayed.',
      :authority_name => raw(@info_request.public_body.name)) %>

<% if @info_request.public_body.is_school? %>
  <%= _('They have not replied to your {{law_used_short}} request {{request_title}} ' \
           'promptly, as normally required by law during term time.',
      :law_used_short => @info_request.law_used_human(:short),
      :request_title => @info_request.title) %>
<% else %>
  <%= _('They have not replied to your {{law_used_short}} request {{request_title}} ' \
           'promptly, as normally required by law.',
      :law_used_short => @info_request.law_used_human(:short),
      :request_title => @info_request.title) %>
<% end %>

<%= _('Click on the link below to send a message to {{authority_name}} reminding ' \
         'them to reply to your request.',
      :authority_name => @info_request.public_body.name) %>

<%= @url %>

-- <%= _('the {{site_name}} team', :site_name=>site_name) %>
