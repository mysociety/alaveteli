<%= raw @message %>

---------------------------------------------------------------------
<%= raw @reported_by.name %> <%= _('has reported an')%> <%= raw @info_request.law_used_human(:short) %>
<%= _('response as needing administrator attention. Take a look, and reply ' \
      'to this email to let them know what you are going to do about it.') %>

Request '<%= raw @info_request.title %>':
<%= @url %>

<%= _('Administration URL:') %>
<%= @admin_url %>
---------------------------------------------------------------------
