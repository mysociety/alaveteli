<div class="row">
  <div class="span12">
    <div class="page-header">
      <h1><%= @title %></h1>
    </div>
  </div>
</div>

<%= render partial: 'show',
           locals: { announcements: @announcements } %>

<%= will_paginate(@announcements, class: 'paginator') %>
