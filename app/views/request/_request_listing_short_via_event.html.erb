<% if @highlight_words.nil?
      @highlight_words = []
end %>

<div class="request_short_listing">
  <h3><%= link_to highlight_words(info_request.title, @highlight_words), request_path(info_request) %></h3>
  <p>
    <%= _('To {{authority_url}}',
          :authority_url => public_body_link(info_request.public_body))%>
    <%= _('by {{user_url}}',
          :user_url => request_user_link(info_request))%>
    <%= simple_date(info_request.created_at) %>
  </p>
</div>
